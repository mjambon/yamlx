; Command-line tool that exercises the yamlx library.
; Reads YAML from stdin or a file and prints the result in the chosen format
; (event tree or pretty-printed YAML).

(executable
 (public_name yamlx)
 (name Main)
 (libraries yamlx)
 (preprocess
  (pps ppx_deriving.show)))
